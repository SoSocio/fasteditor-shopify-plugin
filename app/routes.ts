/**
 * Exports the flatRoutes configuration for Remix file system routing.
 * @returns The result of flatRoutes for use in the app.
 */
import { flatRoutes } from "@remix-run/fs-routes";

export default flatRoutes();
